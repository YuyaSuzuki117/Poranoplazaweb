# Poranoplaza 内装工事 WEBサイト

## プロジェクト概要
- **会社名**: 株式会社ポラーノプラザ
- **種別**: 内装工事会社のコーポレート＋サービス統合サイト
- **目的**: 内装工事の顧客集客に特化、問い合わせ獲得、施工実績による信頼性訴求
- **ターゲット**: 内装工事を求めている事業者（B2B）
- **言語**: 日本語
- **統合元サイト**:
  - 会社HP: https://poranoplaza.com/ （WordPress + Swell / カラー: #204495 濃紺）
  - サービスHP: https://denki-shobou.co.jp/ （WordPress + Elementor + WooCommerce / カラー: #2589D0 ブルー）

## 既存サービス内容
1. **内装工事**（メイン訴求）
2. 空調設備工事
3. 一般電気工事
4. 消防設備工事
- 対応エリア: 一都三県
- 特徴: 見積もりから施工までワンストップ対応、24時間対応
- スローガン: 「作業員から技術者へ」

## 技術スタック
- HTML5 / CSS3（Tailwind CSS） / JavaScript
- レスポンシブデザイン（モバイルファースト）
- 静的サイト（フレームワークは必要に応じて導入）
- motion/react（アニメーション - 必要時のみ）
- cn ユーティリティ（clsx + tailwind-merge）

## ディレクトリ構成
```
/
├── CLAUDE.md
├── index.html
├── css/
│   └── style.css
├── js/
│   └── main.js
├── images/
└── pages/
```

## デザイン方針

### 美的方向性: インダストリアル・プレミアム
- **トーン**: 洗練されたインダストリアル - 内装工事のクラフトマンシップを表現
- **差別化ポイント**: 施工写真を建築雑誌のようなエディトリアルレイアウトで見せる
- **印象**: プロフェッショナル、信頼、技術力、モダン

### タイポグラフィ
- 見出し: 個性的なディスプレイフォント（Google Fontsから選定）
- 本文: Noto Sans JP（可読性重視）
- text-balance（見出し）、text-pretty（本文）を適用
- tabular-nums（数値データ）
- letter-spacing は変更しない

### カラーシステム
- CSS変数で一元管理
- 1ビューにつきアクセントカラーは1色まで
- 紫グラデーション禁止、グラデーション使用は明示的指示時のみ
- グロー効果を主要アフォーダンスにしない
- 既存テーマ / Tailwind デフォルトカラートークン優先

### レイアウト
- Bento Grid（モジュラーレイアウト）を活用
- 非対称・オーバーラップ・グリッドブレイク要素
- 施工写真を大きく見せるエディトリアルレイアウト
- z-index は固定スケール（任意の z-* 禁止）
- h-screen 禁止 → h-dvh を使用
- 正方形要素は size-* を使用

### アニメーション・モーション
- 明示的に要求された場合のみアニメーション追加
- transform と opacity のみアニメーション可（コンポジタプロパティ）
- layout プロパティ（width, height, top, left, margin, padding）のアニメーション禁止
- インタラクションフィードバックは 200ms 以内
- エントランスは ease-out
- prefers-reduced-motion を尊重
- 画面外のループアニメーションは一時停止
- 大きな blur / backdrop-filter のアニメーション禁止
- will-change はアクティブアニメーション中のみ

### 背景・ビジュアル
- ソリッドカラーデフォルトではなく、深みと雰囲気を演出
- ノイズテクスチャ、幾何学パターン、コンクリート質感など文脈に合った表現
- カスタム写真を信頼構築の中心に据える

## コーディング規約

### CSS
- Tailwind CSS デフォルト値を使用（カスタム値は既存 or 明示的要求時のみ）
- cn ユーティリティ（clsx + tailwind-merge）でクラスロジック管理
- Tailwind デフォルトシャドウスケール使用

### HTML
- セマンティックHTML5タグ（header, nav, main, section, footer）
- ネイティブ要素優先（button, a, input）- role ベースのハックより優先
- 見出しレベルをスキップしない
- リストは ul/ol + li

### アクセシビリティ（WCAG準拠）
- 全インタラクティブ要素にアクセシブル名
- アイコンのみボタンは aria-label 必須
- 全 input/select/textarea にラベル必須
- Tab で全インタラクティブ要素到達可能
- tabindex > 0 禁止
- モーダルはフォーカストラップ、Escape で閉じる
- エラーは aria-describedby でフィールドに紐付け
- required フィールドはアナウンス
- コントラスト比 4.5:1 以上
- hover のみインタラクション禁止（キーボード代替必須）
- 画像 alt 必須（装飾画像は aria-hidden）
- prefers-reduced-motion 対応

### メタデータ（SEO）
- 全ページに title + meta description
- canonical URL 設定
- OGP（og:title, og:description, og:image）設定
- twitter:card 設定（summary_large_image）
- html lang="ja"
- favicon + apple-touch-icon
- JSON-LD 構造化データ（実在コンテンツのみ）
- 重複メタタグ禁止
- og:url と canonical を一致させる

### パフォーマンス
- 画像: WebP 優先、適切な srcset/sizes
- will-change はアクティブアニメーション外で使用禁止
- 大きな blur() / backdrop-filter アニメーション禁止
- useEffect はレンダーロジックで表現できるなら使わない
- 軽量・高速なページロード

### フォーム
- input/textarea で paste をブロックしない
- AlertDialog で破壊的アクション確認
- エラーはアクション発生箇所に表示
- ローディングは構造スケルトン推奨
- XSS 対策を徹底

## ページ構成（予定）
1. **トップページ** - ヒーロー、サービス概要、施工実績ハイライト、選ばれる理由、お客様の声、CTA
2. **サービス紹介** - 内装工事の種類と詳細、対応範囲
3. **施工実績** - エディトリアル・ギャラリー形式
4. **会社概要** - 企業情報、資格保有者、アクセス
5. **お問い合わせ** - フォーム + 電話CTA

## 2026年 取り入れるデザイントレンド
- Bento Grid レイアウト（モジュラー・適応型）
- スクロールトリガーアニメーション（控えめ・パフォーマンス重視）
- カスタム写真による信頼構築
- 戦略的テスティモニアル配置（各ページに分散）
- グリッドブレイクレイアウト
- ダークモード対応検討
- マイクロインタラクション（200ms以内のフィードバック）

## コンテキスト管理（最重要）

コンテキスト溢れを防ぐため、以下を厳守する。

### 応答の簡潔さ
- 回答は要点のみ。冗長な説明・繰り返し・前置きを省く
- コード変更の報告は「何を変えたか」の箇条書き3-5行で完結
- 変更していない箇所の解説は一切不要
- ファイル全体をチャットに出力しない（Editツールで差分のみ）
- 同じ説明を別の言い方で繰り返さない

### ファイル読み込みの最小化
- 大きなファイル（150行超）は offset/limit で必要箇所のみ読む
- index.html は構造確認なら先頭80行、特定セクションならGrep→offset指定
- 同じファイルを会話中に再読み込みしない（1回読んだら記憶で作業）
- 編集前の確認読みは変更対象の周辺30行で十分

### サブエージェント活用（必須）
- 3ファイル以上のリサーチ → Task（Explore）に委任
- 参考サイト・競合調査 → Task に委任し要点のみ受け取る
- 独立した調査は並列起動（1つずつ順番にやらない）
- WebFetch結果が長い場合、サブエージェント内で処理して要約だけ返す

### 大きな作業の分割
- 大規模変更（3ファイル超 or 100行超の変更）→ プランモード必須
- 1ターンで変更するファイルは最大3つを目安
- セクション単位で実装→確認→次へ の流れを守る

### コンテキスト節約テクニック
- Globの結果が大量の場合、head_limitで絞る
- Grepは files_with_matches モードをデフォルトに（content は必要時のみ）
- 画像ファイルのリストは必要な時だけ取得
- git log は --oneline -5 程度に留める

## 作業ルール
- 変更前に必ず既存コードを読んでから編集する（ただし必要最小限の範囲）
- 1つの作業単位ごとにタスクリストで管理する
- 大きな変更はプランモードで設計してから実装する
- ファイル作成は最小限に、既存ファイルの編集を優先する
- セキュリティ（XSS等）に注意してフォーム実装する
- コンポーネントプリミティブは既存のものを優先使用
- アニメーションライブラリの移行はユーザー明示的要求時のみ

## 内装工事WEBデザイン開発ワークフロー

### デザイン変更の手順
1. 変更対象セクションのみをGrepで特定（HTMLのid/class検索）
2. 対象セクションの周辺コードのみ読み込み（offset/limit活用）
3. Editで差分変更（Write禁止 — 既存ファイル上書きリスク）
4. 変更完了報告は箇条書き3行以内

### 施工写真の扱い
- 写真パスは images/projects/ 配下（MEMORY.md参照）
- 新規写真追加時は既存のディレクトリ構成を踏襲
- 画像最適化（WebP変換・リサイズ）は別タスクとして切り出す
- B/Aスライダーは img-comparison-slider を使用（統一済み）

### ページ間の一貫性
- ヘッダー/フッター/ナビの変更は全5ページに反映（index.html + pages/*.html）
- 共通変更は Task で並列実行可能
- カラー・フォントの変更は css/input.css のCSS変数を変更（個別ファイルにハードコード禁止）

### デプロイ確認
- GitHub Pages: main ブランチ push で自動デプロイ
- デモ確認: https://yuyasuzuki117.github.io/Poranoplazaweb/
- push前にローカルでHTML構文チェック推奨
